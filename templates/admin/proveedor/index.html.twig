{% extends 'base.html.twig' %}

{% block title %}Proveedor index{% endblock %}

{% block body %}
    <h1>Proveedor index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Nombre</th>
                <th>Telefono</th>
                <th>Email</th>
                <th>Notas</th>
                <th>Activo</th>
                <th>Cif</th>
                <th>Direccion</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for proveedor in proveedors %}
            <tr>
                <td>{{ proveedor.id }}</td>
                <td>{{ proveedor.nombre }}</td>
                <td>{{ proveedor.telefono }}</td>
                <td>{{ proveedor.email }}</td>
                <td>{{ proveedor.notas }}</td>
                <td>{{ proveedor.activo ? 'Yes' : 'No' }}</td>
                <td>{{ proveedor.cif }}</td>
                <td>{{ proveedor.direccion }}</td>
                <td>
                    <a href="{{ path('app_proveedor_show', {'id': proveedor.id}) }}">show</a>
                    <a href="{{ path('app_proveedor_edit', {'id': proveedor.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="9">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_proveedor_new') }}">Create new</a>
{% endblock %}
